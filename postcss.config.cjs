// Use the dedicated PostCSS package for Tailwind per the new plugin location
// See: https://tailwindcss.com/docs/installation/postcss
module.exports = {
	plugins: {
		// Tailwind removed — keep autoprefixer only
		autoprefixer: {},
	},
}
